[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "--json-report --json-report-file=../reports/backend.json"
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py313"
