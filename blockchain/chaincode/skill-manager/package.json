{
  "name": "skill-manager",
  "version": "2.1.0",
  "description": "SynapseNet Skill Management Chaincode",
  "main": "dist/index.js",
  "scripts": {
    "start": "fabric-chaincode-node start",
    "start:server": "fabric-chaincode-node server --chaincode-address=${CHAINCODE_ADDRESS:-0.0.0.0:9999} --chaincode-id=${CHAINCODE_ID}",
    "build": "tsc",
    "test": "npm run build && node --test test/*.test.js"
  },
  "keywords": [],
  "author": "SynapseNet Team",
  "license": "ISC",
  "dependencies": {
    "fabric-contract-api": "^2.5.6",
    "fabric-shim": "^2.5.6",
    "json-stringify-deterministic": "^1.0.12",
    "sort-keys-recursive": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
