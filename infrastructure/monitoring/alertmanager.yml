route:
  receiver: production-operations
  group_by: [alertname, job]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: production-operations
    webhook_configs:
      - url_file: /run/secrets/alert_webhook_url
        send_resolved: true
