# Skill Matcher Service

This directory reserves the deployable boundary for the future AI-assisted skill-matching
service. Its Python dependencies belong in `requirements.txt`; source and a container
definition should be added together when the service is implemented.
